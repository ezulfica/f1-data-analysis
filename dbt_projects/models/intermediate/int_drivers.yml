version: 2

models:
  - name: int_drivers
    description: Intermediate table for the drivers dimension
    columns:
      - name: driver_id
        # tests:
        #   - unique
    config:
      materialized: view
      unique_key: driver_id